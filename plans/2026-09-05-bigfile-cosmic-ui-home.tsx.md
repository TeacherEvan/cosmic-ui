# Refactor plan — `src/pages/home.tsx` in `TeacherEvan/cosmic-ui`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-05T03:11:25.543922+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/cosmic-ui` |
| path | `src/pages/home.tsx` |
| size | 77.0 KB (78,883 bytes) |
| lines | 1,004 |
| generated | 2026-09-05T03:11:25.543939+07:00 |

### Top imports (sample)
  - `import {`
  - `import { Link } from "react-router";`
  - `import { twMerge } from "tailwind-merge";`
  - `import { Frame } from "@/components/ui/frame";`
  - `import { ChevronLeft, ChevronRight } from "lucide-react";`
  - `import { Button } from "@/components/ui/button";`
  - `import { Chart, getColor } from "@/components/ui/chart";`
  - `import { FilePenLine, CopySlash } from "lucide-react";`

## Structural analysis

**Render branches:** 1 `return (` blocks at L17

**Repeated literals (≥2×):**
- `var(--color-frame-1-stroke)` (×39)
- `var(--color-frame-1-fill)` (×39)
- `[&.first-date]:[--color-frame-1-stroke:var(--color-accent)] ` (×35)
- `[&.second-date]:[--color-frame-1-stroke:var(--color-accent)]` (×35)
- `h-full -inset-x-5 inset-y-0 absolute group-[.first-date]:ins` (×35)
- `relative group-[.first-date]:font-bold group-[.first-date]:t` (×35)
- `cursor-pointer group text-center py-1.5 relative overflow-hi` (×27)
- `[--color-frame-1-stroke:var(--color-primary)]/50` (×4)

**Top-referenced imports:**
- `twMerge` (41 refs)
- `Frame` (40 refs)
- `Button` (13 refs)
- `getColor` (7 refs)
- `Link` (5 refs)
- `Chart` (4 refs)
- `ChevronLeft` (2 refs)
- `ChevronRight` (2 refs)
- `FilePenLine` (2 refs)
- `CopySlash` (2 refs)

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `cosmic-ui`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Hoist repeated literal `'var(--color-frame-1-stroke)'` (×39) to a constant
- **Target:** String literal appears 39× in `src/pages/home.tsx`. Extract to
  `./constants.ts` (already exists per imports) or co-located `strings.ts`.
- **Why:** Repeated literals indicate copy that should be localizable or reused.
- **Acceptance:** zero occurrences of the literal in `src/pages/home.tsx` after the change;
  constant referenced via the barrel.
- **Validation:** `rg -F '<literal>' src/pages/home.tsx` returns 0.
- **Evidence:** grep before/after.

### OBJ-002 — Confirm `twMerge` (41 refs) is the right shared utility
- **Target:** `twMerge` is the most-referenced import in this file (41 uses).
- **Action:** verify whether other dashboard pages also import it; if so, ensure it lives
  in a shared `lib/` or `@/components/ui/` and not in a feature-local path.
- **Acceptance:** document the canonical home for `twMerge` in `docs/`.
- **Validation:** `rg -l 'twMerge' src/` — should match ≤3 call sites in
  feature code, more in shared.
- **Evidence:** call-site map.

### OBJ-003 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `src/pages/home.tsx` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `src/pages/home.tsx`.
- **Validation:** `rg -n '^import' src/pages/home.tsx` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-004 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `src/pages/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-005 — Reduce `src/pages/home.tsx` below 502 lines via cumulative extractions
- **Target:** After OBJ-001 → OBJ-008 land, the remaining render block should
  be a thin composition of named sub-components.
- **Goal:** `src/pages/home.tsx` shrinks from 1,004 to ≤502 lines.
- **Acceptance:** `wc -l` shows the reduction; behavior unchanged via tests + e2e.
- **Validation:** `pnpm run build && pnpm run test && pnpm run test:e2e` (full gate).
- **Evidence:** before/after line count + green build.

### OBJ-006 — Hardening pass 6
- **Target:** Tighten types / tighten prop drilling in `src/pages/home.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `src/pages/home.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `src/pages/home.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `src/pages/home.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `src/pages/home.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `src/pages/home.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `src/pages/home.tsx` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `src/pages/home.tsx` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `cosmic-ui`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-05-bigfile-cosmic-ui-home.tsx.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.


## REVIEW 2026-09-05T03:48:41.533802+07:00

**Verdict:** `READY-WITH-WARNINGS`

**Structural check:** objectives=12 file_header=✓ imports=✓ why=✓ dod=✓ security=✓

**Gaps:**
1. llm_unreachable
